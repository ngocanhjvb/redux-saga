import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
    box: {
        display: 'flex',
        alignItems: 'center',
    },
    shape: {
        padding: 20,
        margin: 10,
        backgroundColor: 'red',
    },

}));
